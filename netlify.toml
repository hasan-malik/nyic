[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (/stories/:id, /admin, /constellation…)
# resolve on direct load / refresh instead of 404ing.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
